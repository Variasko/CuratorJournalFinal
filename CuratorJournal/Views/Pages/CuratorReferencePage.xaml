<Page x:Class="CuratorJournal.Views.Pages.CuratorReferencePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CuratorJournal.Views.Pages"
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CuratorReferencePage">
    <Border Background="White" CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect Color="LightGray" Opacity="0.5" BlurRadius="20"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <DataGrid x:Name="MainDataGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding Person.Surname}" Width="*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Person.Name}" Width="*"/>
                    <DataGridTextColumn Header="Отчество" Binding="{Binding Person.Patronymic}" Width="*"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding TeacherCategory.Name}" Width="*"/>
                    <DataGridTextColumn Header="Логин" Binding="{Binding User.Login}" Width="*"/>
                    <DataGridTemplateColumn Header="Действия" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Name="EditButton" 
                                            Background="Transparent"
                                            Click="EditButton_Click">
                                        <fa:ImageAwesome Icon="Edit" Width="16" Height="16"/>
                                    </Button>
                                    <Button Name="DeleteButton" 
                                            Background="Transparent"
                                            Click="DeleteButton_Click"
                                            Margin="5,0,0,0">
                                        <fa:ImageAwesome Icon="Trash" Width="16" Height="16"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Button Name="CreateButton"
                    Content="Добавить"
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    Click="CreateButton_Click"/>
        </Grid>
    </Border>
</Page>